services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .e2e.env
    ports:
      - "${APP_REST_PORT:-8000}:${APP_REST_PORT:-8000}"
    volumes:
      - .:/app
      - ./logs:/app/logs
    command: ./user-service
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - service-network

    healthcheck:
      test:
        [
          "CMD-SHELL",
          "curl -f http://localhost:${APP_REST_PORT:-8000}/health || exit 1",
        ]
      interval: 5s
      timeout: 3s
      retries: 5

  migrate:
    build:
      context: .
      dockerfile: Dockerfile.migrate
    container_name: migrate
    command: ["up"]
    env_file:
      - .e2e.env
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - service-network

  postgres:
    image: postgres:latest
    env_file: .e2e.env

    volumes:
      - postgres_data_tests:/var/lib/postgresql

    command: |
      postgres
        -c log_statement=all
        -c log_min_duration_statement=0
        -c log_connections=on
        -c log_disconnections=on

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - service-network

volumes:
  postgres_data_tests:

networks:
  service-network:
    driver: bridge
